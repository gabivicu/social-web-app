{% block form_errors %}
    {% if errors|length > 0 %}
        <ul class="mt-1 list-none p-0">
            {% for error in errors %}
                <li class="text-red-500 text-sm">{{ error.message }}</li>
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}